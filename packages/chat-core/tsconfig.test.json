{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests may use node: portability.test.ts reads the source tree to prove
    // the source itself never does.
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": []
}
